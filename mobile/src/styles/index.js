/* Importa todos os arquivos de estilo e exporta tudo de uma só vez */

import colors from './colors';
import fonts from './fonts';
import metrics from './metrics';
import general from './general';

export {
    colors,
    fonts,
    metrics,
    general
};